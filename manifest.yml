#--------------------------------------
# change the `host` property to a
# unique hostname on the domain you
# are deploying to.
#--------------------------------------

applications:
- name:    congratserver
  host:    congratserver-llang
  memory:  64M
  command: node congratserver.js
